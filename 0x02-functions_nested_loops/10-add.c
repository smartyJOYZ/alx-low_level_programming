#include "main.h"
#include <stdio.h>

/**
 * add - a function that prints the last digit of a number
 * @a : is the char to be checked
 * @b: a number
 * Return: always 0
 */
int add(int a, int b)
{
	int add;

	add = a + b;
	return (add);
}
